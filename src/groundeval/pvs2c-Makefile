# ==========================================
# 1. CONFIGURATION
# ==========================================
CC       := cc
PVSPATH  := <pvspath>
CFLAGS   := -w -O2 -foptimize-sibling-calls -fPIC -I$(PVSPATH)/lib/pvs2c/include -I include
LDFLAGS  := -L$(PVSPATH)/lib/pvs2c/lib
LDLIBS   := -lgmp -lpvs-prelude
BINDIR   := bin
LIBDIR   := lib
SRCDIR   := src

# ==========================================
# 2. INCLUDE DYNAMIC FRAGMENTS
# ==========================================
-include include/*.mk

# ==========================================
# 3. GENERIC RULES
# ==========================================
.PHONY: all clean

# Ensure build directories exist
$(BINDIR) $(LIBDIR):
	mkdir -p $@

# Standard C compilation -> output to BUILDDIR
$(SRCDIR)/%.o: %.c | $(SRCDIR)
	$(CC) $(CFLAGS) -c $< -o $@

# ==========================================
# 4. SMART CLEAN
# ==========================================
clean:
	@echo "--- Cleaning ---"
	rm -rf $(BUILDDIR) $(BINDIR) $(LIBDIR)
