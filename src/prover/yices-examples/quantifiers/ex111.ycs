
(define-type Point (record x::int y::int))
(define f::(-> int int))
(assert (forall (p::Point) (= (mk-record x::(f (select p y)) y::(f (select p x)))
                              (mk-record y::0 x::1))))
(define p1::Point)
(assert (= (f (select p1 x)) 1))
(check)
