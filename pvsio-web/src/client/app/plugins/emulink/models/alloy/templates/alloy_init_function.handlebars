{{#if template_description}}
%
%   Description: This template generates the alloy function for initialising the state of the system.
%
{{/if}}{{#if comment}}/**
 * {{comment}}
 */
{{/if}}fact { {{#each variables}}
    first.{{name}} = {{value}}{{/each}}
}
