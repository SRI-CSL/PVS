{{#if template_description}}
%
%   Description: This template generates alloy enumerated types using the following syntax:
%                  abstract sig <enum_type_name> {}
%                  one c1, c2, ... extends <enum_type_name> {}
%
{{/if}}{{#if comment}}/**
   * {{comment}}
   */{{/if}}
{{#each enumtype}}{{#if modes}}abstract sig {{type}} {}
one sig {{#each modes}}{{this}}{{#unless @last}}, {{/unless}}{{/each}} extends {{../type}} {}{{/if}}{{/each}}
