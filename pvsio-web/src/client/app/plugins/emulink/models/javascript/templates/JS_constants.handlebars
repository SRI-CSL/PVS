{{#if template_description}}
%
%   Description: This template generates JavaScript constants:
%                - const const_name = const_val;
%
{{/if}}{{#if comment}}        /**
         * {{comment}}
         */{{/if}}{{#each constants}}
        const {{name}} = {{value}};{{/each}}
