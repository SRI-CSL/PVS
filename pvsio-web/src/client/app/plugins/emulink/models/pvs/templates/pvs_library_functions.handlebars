{{#if template_description}}
%
%   Description: This template generates PVS library functions in the form
%                  foo(arg1: type1, arg2: type2, ...): return_type =
%                    COND
%                      ...
%                    ENDCOND
%
{{/if}}{{#if comment}}  %-- {{comment}}{{/if}}
{{#each functions}}  {{name}}{{#if args}}({{#each args}}{{name}}: {{type}}{{#unless @last}}, {{/unless}}{{/each}}){{/if}}: {{return_type}} =
    COND{{#each cases}}
    {{cond}}
     -> {{return_expr}}{{#unless @last}},{{/unless}}{{/each}}
    ENDCOND{{#unless @last}}

{{/unless}}{{/each}}
